fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9  # Required for UploadFile / PDF ingest endpoint
pydantic>=2.8
pydantic-settings>=2.5
httpx>=0.27.0
structlog>=24.0
anthropic>=0.34.0
pypdf>=4.0.0            # PDF text extraction fallback (pure Python)
pdfplumber>=0.11.0      # Primary PDF parser — preserves tables and layout
redis[asyncio]>=5.0.0   # Strategy profile storage
prometheus-fastapi-instrumentator>=0.21.0

# ── Test / dev dependencies ───────────────────────────────────────────────────
pytest>=9.0
pytest-asyncio>=0.23
